# Container runtime: docker or podman (auto-detected if unset)
#JAILED_RUNTIME=

# Image name/tag
#JAILED_IMAGE=jailed:latest

# Username inside container (must match what image was built with)
#JAILED_USER=coder

# Override config directory (default: $XDG_CONFIG_HOME/jailed or ~/.config/jailed)
#JAILED_CONFIG_DIR=

# Sync strategy: mutagen or bind
#JAILED_SYNC_STRATEGY=mutagen
